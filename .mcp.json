{
  "mcpServers": {
    "nostr-signer": {
      "title": "Nostr Signer",
      "description": "Pair a user-controlled NIP-46 signer, approve signatures, and publish verified Nostr events without handling an nsec.",
      "command": "node",
      "args": ["--env-file-if-exists=.env", "./mcp/server.mjs"],
      "cwd": "."
    }
  }
}
